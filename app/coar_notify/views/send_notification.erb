<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Send Notification - COAR Notify</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 min-h-screen">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <!-- Back Button -->
    <div class="mb-6">
      <a href="/coar_notify/dashboard" class="inline-flex items-center text-sm text-gray-600 hover:text-gray-900">
        <svg class="w-5 h-5 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/>
        </svg>
        Back to Dashboard
      </a>
    </div>

    <!-- Header -->
    <div class="mb-8">
      <h1 class="text-3xl font-bold text-gray-900">Send Notification</h1>
      <p class="mt-2 text-gray-600">Send a COAR Notify notification to a service</p>
    </div>

    <!-- Form -->
    <div class="bg-white rounded-lg shadow-lg">
      <form method="POST" action="/coar_notify/dashboard/send" class="p-6 space-y-6">

        <!-- Step 1: Issue ID -->
        <div class="border-b border-gray-200 pb-6">
          <h2 class="text-lg font-semibold text-gray-900 mb-4">1. Select Preprint</h2>
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">
              Issue ID <span class="text-red-600">*</span>
            </label>
            <input type="number" name="issue_id" required
                   class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 text-lg"
                   placeholder="e.g., 123">
            <p class="mt-2 text-sm text-gray-500">
              The GitHub issue ID for the preprint. Paper DOI and URL will be automatically computed.
            </p>
          </div>
        </div>

        <!-- Step 2: Select Pattern -->
        <div class="border-b border-gray-200 pb-6">
          <h2 class="text-lg font-semibold text-gray-900 mb-4">2. Select Notification Type</h2>
          <div class="space-y-3">
            <label class="flex items-start p-4 border-2 border-gray-200 rounded-lg cursor-pointer hover:border-blue-500 hover:bg-blue-50 transition-all">
              <input type="radio" name="pattern" value="RequestReview" class="mt-1 mr-3" required>
              <div>
                <div class="font-medium text-gray-900">Request Review</div>
                <div class="text-sm text-gray-600">Request a peer review for this preprint</div>
              </div>
            </label>

            <label class="flex items-start p-4 border-2 border-gray-200 rounded-lg cursor-pointer hover:border-blue-500 hover:bg-blue-50 transition-all">
              <input type="radio" name="pattern" value="RequestEndorsement" class="mt-1 mr-3">
              <div>
                <div class="font-medium text-gray-900">Request Endorsement</div>
                <div class="text-sm text-gray-600">Request an endorsement for this preprint</div>
              </div>
            </label>
          </div>
        </div>

        <!-- Step 3: Select Service -->
        <div class="border-b border-gray-200 pb-6">
          <h2 class="text-lg font-semibold text-gray-900 mb-4">3. Select Target Service</h2>
          <select name="service" required
                  class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 text-lg">
            <option value="">-- Select a service --</option>
            <option value="local_test">🧪 Local Test (localhost)</option>
            <option value="elife">eLife</option>
            <option value="review_commons">Review Commons</option>
            <option value="pci">PCI (Peer Community In)</option>
            <option value="prereview">PREreview</option>
          </select>
          <p class="mt-2 text-sm text-gray-500">
            The service will receive the notification at their configured inbox URL.
          </p>
        </div>

        <!-- Step 4: Optional Notes -->
        <div class="pb-6">
          <h2 class="text-lg font-semibold text-gray-900 mb-4">4. Additional Notes (Optional)</h2>
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">
              Message / Notes
            </label>
            <textarea name="notes" rows="4"
                      class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
                      placeholder="Any additional context or notes for the review/endorsement request..."></textarea>
          </div>
        </div>

        <!-- Submit Button -->
        <div class="flex items-center justify-end gap-4 pt-4">
          <a href="/coar_notify/dashboard"
             class="px-6 py-3 border border-gray-300 rounded-lg text-gray-700 font-medium hover:bg-gray-50 transition-colors">
            Cancel
          </a>
          <button type="submit"
                  class="px-6 py-3 bg-blue-600 text-white font-medium rounded-lg hover:bg-blue-700 transition-colors flex items-center gap-2">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8"/>
            </svg>
            Send Notification
          </button>
        </div>
      </form>
    </div>

    <!-- Info Box -->
    <div class="mt-6 bg-blue-50 border border-blue-200 rounded-lg p-4">
      <div class="flex">
        <div class="flex-shrink-0">
          <svg class="h-5 w-5 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
          </svg>
        </div>
        <div class="ml-3">
          <h3 class="text-sm font-medium text-blue-800">About COAR Notify</h3>
          <div class="mt-2 text-sm text-blue-700">
            <p>These notifications follow the COAR Notify specification v1.0.1 for scholarly communication. The paper's DOI and URL are automatically derived from the issue ID.</p>
            <a href="https://coar-notify.net/specification/1.0.1/" target="_blank" class="underline mt-1 inline-block">
              Learn more about COAR Notify →
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
